build:
  environment:
        php: 7.4.16
  nodes:
    coverage:
      tests:
        override:
          - command: make test
            coverage:
              file: build/coverage.xml
              format: clover
    analysis:
      tests:
        override:
          - php-scrutinizer-run
